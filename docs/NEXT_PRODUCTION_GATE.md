# Next Production Gate

After Master Asset Architecture v0.1 merges:

1. create `asset/reference-character-proof-v0.1` from main;
2. build/review the first L1 visual source for NT-CHAR-001;
3. generate one representative Card/2D adaptation from the derived context;
4. design the Print adaptation engineering concept from the same upstream identity;
5. record where the two media require different transformations;
6. run schema/dependency checks;
7. only after printable geometry exists, perform slice → physical print → assembly evidence;
8. fill `REFERENCE_PROOF_RESULT_TEMPLATE.md` and decide PASS/REVISE.

Do not add a second reference character until the first proof produces a concrete reason.
