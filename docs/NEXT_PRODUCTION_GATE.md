# Next Production Gate — NT-CHAR-001

The architecture documentation phase is sufficiently specified for a real artifact test.

## Gate 1 — 2D

Generate one neutral 3/4 depiction from `docs/2D_ADAPTATION_BRIEF_NT-CHAR-001.md` and review it against the trace matrix.

Then generate/derive a second substantially different pose/orbit orientation. If both remain the same identity without manually re-describing it, the 2D adapter passes its first coherence test.

## Gate 2 — Physical

Create actual geometry for the same identity, then slice against the A1 mini constraint and perform physical calibration/assembly before declaring print standards.

## Stop condition

If either adapter requires changing a declared identity invariant merely to become usable, stop and revise the master/adaptation boundary before roster expansion.
