# Branch Note

`design/master-asset-architecture-v0.1-clean` was created directly from current `main` to keep the architecture PR focused. An older experimental branch with a similar name existed and was intentionally not used because it had accumulated excessive speculative commits.
