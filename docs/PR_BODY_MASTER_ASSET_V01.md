## Purpose
Establish the minimal source-of-truth architecture for a Character/IP-first NOVA TAILS asset system before scaling roster or committing deeply to game/print/content lanes.

## Core model
`L0 IP Canon -> L1 Master Design -> L2 Adapters -> L3 Deliverables`

Single source of truth means a shared identity/design contract, not one universal binary mesh/image.

## Included
- strategic IP-first asset-platform direction under REVIEW;
- master asset authority/layer rules;
- minimal Character Identity JSON Schema;
- one DRAFT reference identity (`NT-CHAR-001`, Spirit-derived architecture stress case);
- card/2D and print adapter manifests pinned to the same identity revision;
- adapter contracts and dependency/version semantics;
- compatible vs identity-breaking dependency exercise;
- AI context derivation/drift rules;
- 2D and print production-proof briefs;
- risk register, trace matrix and acceptance gates;
- P01 real exploratory result (`n=1`) with explicit evidence boundary.

## Explicitly not validated
- reference character as production canon/flagship;
- multi-output market demand;
- game mechanics/product choice;
- print manufacturability/tolerances;
- architecture at roster scale.

## Review gate
Architecture documentation/contract is internally PASS for production proof. Scaling remains blocked until actual cross-output artifacts are produced and reviewed.

## Next after merge
Create `asset/reference-character-proof-v0.1`, generate controlled 2D adaptations from the same identity context, then create/slice/physically test a print adaptation before allowing roster expansion.
