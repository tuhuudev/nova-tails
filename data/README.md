# Structured Asset Data

Only create structured instances here when a real retained identity/adaptation exists.

- `characters/` contains L0 character identity instances validated by `schemas/character-identity.schema.json`.
- `adapters/` contains L2 adaptation manifests validated by `schemas/adapter-manifest.schema.json`.

Files marked DRAFT/reference are test fixtures and are not production canon merely because they are machine-readable.
