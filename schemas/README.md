# Schemas

v0.1 contains only contracts justified by current work:

- `character-identity.schema.json` — L0 identity authority.
- `adapter-manifest.schema.json` — L2 medium adaptation dependency/validation manifest.

Do not add exhaustive lore, combat, asset-registry or manufacturing schemas until real retained data requires them.
